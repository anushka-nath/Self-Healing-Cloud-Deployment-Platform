apiVersion: v1
kind: Service
metadata:
  name: {{ include "self-healing-api.fullname" . }}
  labels:
    {{- include "self-healing-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "self-healing-api.selectorLabels" . | nindent 4 }}
